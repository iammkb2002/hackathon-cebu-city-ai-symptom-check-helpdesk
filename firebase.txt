NOTE: Update this file to be exactly the same as the actual firebase configurations for consistent context

collections:


rules:
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No access to any collections
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

